import pandas as pd

# Dummy
def finger_distance(df: pd.DataFrame) -> pd.DataFrame:
    """
    _Beispiel_
    Nimmt die Koordinaten der Finger, berechnet die Abstände und gibt diese jeweils als neues Feature als
    DataFrame zurück.

    :param df:
    :return: Features für jede Spalte
    """
    return None

